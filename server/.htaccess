# Protect server directory
Options -Indexes

# Deny access to sensitive file types
<FilesMatch "\.(json|log|sql|env|ini)$">
    Order allow,deny
    Deny from all
</FilesMatch>

# Allow access to API folder
<IfModule mod_rewrite.c>
    RewriteEngine On
</IfModule>
